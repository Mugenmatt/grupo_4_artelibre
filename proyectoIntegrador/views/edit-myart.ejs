<!DOCTYPE html>
<html lang="en">

<%- include('partials/head', {title: 'Perfil', css: 'edit-myart.css'}) %>

<body>

    <div class="hero">
        <%- include('partials/header') %>
    </div>

<main>

    <h6 class="container">
        <a class="linkprofile" href="/users/profile/myart"><i class="material-icons">navigate_before</i>Volver a mis publicaciones</a>
    </h6>

    <div id="test2" class="col s12">
        
        <form enctype="multipart/form-data" method="POST" action="/users/profile/myart/<%= product.id %>?_method=PUT"
            class="col s12">
            <div class="row">
                <div class="input-field col s12 m6">
                    <input id="first_name" type="text" class="validate" name="name" value="<%= product.name %>">
                    <label for="first_name">Titulo de la obra</label>
                </div>
                <div class="col s12 m6 file-field input-field">
                    <div class="btn">
                        <span>File</span>
                        <input type="file" name="imageFile">
                    </div>
                    <div class="file-path-wrapper">
                        <input class="file-path validate" type="text" placeholder="<%= product.imageFile %>">
                    </div>
                </div>
            </div>
            <div class="row">
                <div class="input-field col s12">
                    <textarea id="descripcion" class="materialize-textarea"
                        name="description"> <%= product.description %> </textarea>
                    <label for="descripcion">Descripción</label>
                </div>
            </div>
            <div class="row">
                <div class="input-field col s12 m4">
                    <input id="price" type="number" class="validate" name="price" value="<%= product.price %>">
                    <label for="price">Precio</label>
                </div>
                <div class="input-field col s12 m4">
                    <input id="quantity" type="number" class="validate" name="quantity" value="<%= product.quantity %>">
                    <label for="quantity">Cantidad</label>
                </div>
                <div class="col s12 m4 tamaño-cuadro">
                    <div class="col s6 input-field ancho">
                        <input id="ancho" type="number" class="validate" name="ancho" value="<%= product.ancho %>">
                        <label for="ancho">Ancho</label>
                    </div>
                    <div class="col s6 input-field alto">
                        <input id="alto" type="number" class="validate" name="alto" value="<%= product.alto %>">
                        <label for="alto">Alto</label>
                    </div>
                </div>
            </div>
            <div class="row">
                <div class="input-field col s12">
                    <textarea id="keywords" class="materialize-textarea"></textarea>
                    <label for="keywords">keywords</label>
                </div>
            </div>
            <div class="row">
                <div class="input-field col s12 m6">
                    <select>
                        <option value="0" <% if (product.status == 0) { %> selected <% } %> >Disponible</option>
                        <option value="1" <% if (product.status == 1) { %> selected <% } %>>Vendido</option>
                        <option value="2" <% if (product.status == 2) { %> selected <% } %>>En pausa</option>
                    </select>
                    <label>Estado</label>
                </div>
                <div class="row s12 m6 right-align">
                    <button type="reset" class="btn waves-effect waves-light btn-medium red boton-resetear">
                        <i class="material-icons">replay</i>
                    </button>
                    <button type="submit" class="btn waves-effect waves-light btn-medium boton-guardar">
                        Guardar <i class="material-icons right">check</i>
                    </button>
                </div>
            </div>
        </form>
    </div>


</main>

    <%- include('partials/footer.ejs') %>

    <script src="https://code.jquery.com/jquery-3.5.1.min.js"
        integrity="sha256-9/aliU8dGd2tb6OSsuzixeV4y/faTqgFtohetphbbj0=" crossorigin="anonymous"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/js/materialize.min.js"></script>
    <script src="/js/header-function.js"></script>
    <script src="/js/editmyart-function.js"></script>

</body>

</html>